package com.example.glidedemo.foreground.services

/**
 * android.permission.START_FOREGROUND_SERVICES_FROM_BACKGROUND
 * android 12
 * 用于允许应用在后台启动前台服务。这项权限主要用于增强后台服务的启动能力，特别是在应用需要在后台执行重要的操作时，例如与系统或其他应用的交互。
 */
class StartFromBackgroundService {
}